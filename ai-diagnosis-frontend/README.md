# AI Diagnosis Frontend

极简诊断页面，使用 Anime.js 做加载、结果切换和链路流动动效。

## 启动

```bash
npm install
npm run dev
```

默认访问：

```text
http://127.0.0.1:5173
```

页面默认连接：

```text
http://localhost:9001
```

如果后端端口或地址不同，可以在页面的「后端地址」输入框里修改。
